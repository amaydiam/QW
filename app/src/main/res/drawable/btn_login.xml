<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" >
    <corners
        android:radius="5dp"/>
    <!-- Padding, in this case, around the button's text  -->
    <padding
        android:left="20dp"
        android:right="20dp"
        android:top="10dp"
        android:bottom="10dp"
        />
    <solid
        android:color="@color/blue_2196F3"/>
    <stroke
        android:color="@color/mdtp_date_picker_view_animator"
        android:width="2dp" />
</shape>